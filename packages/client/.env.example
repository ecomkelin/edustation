# EduStation Client 运行时环境变量
# 复制为 .env.local 后填入真实值

# 后端 API 前缀（H5 用相对路径时由 vite proxy 处理；小程序/App 必须是绝对地址）
VITE_API_BASE_URL=https://api.your-domain.com/api/v1

# 是否启用 mock 模式（本地未起后端时使用 mock 数据）
VITE_USE_MOCK=false
